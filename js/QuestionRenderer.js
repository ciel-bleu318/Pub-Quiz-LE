/* ============================================================
   QuestionRenderer — renders Modus 3 (Frageseite)
   Stub for now; built out in Mode 3 step.
   ============================================================ */

const QuestionRenderer = (function () {
    function init(rootEl) {
        rootEl.innerHTML = '<p style="color:var(--text-dim); padding:40px;">// FRAGESEITE — Modus 3 wird in Schritt 3 implementiert.</p>';
    }
    function render(categoryId, questionId) {
        // implemented in Mode 3
    }
    return { init, render };
})();
